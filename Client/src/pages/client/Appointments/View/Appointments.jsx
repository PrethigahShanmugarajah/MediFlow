// MediFlow / Client / src / pages / client / Appointments / Appointments.jsx
import AppointmentsPage from "../../../../components/AppointmentsPage/AppointmentsPage";

const Appointments = () => {
  return (
    <div>
      <AppointmentsPage />
    </div>
  );
};

export default Appointments;
