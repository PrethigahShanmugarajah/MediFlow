// MediFlow / Client / src / Doctors / components / DoctorNavbar / DoctorNavbar.jsx

const DoctorNavbar = () => {
  return (
    <div>
      <h1>DoctorNavbar</h1>
    </div>
  );
};

export default DoctorNavbar;
