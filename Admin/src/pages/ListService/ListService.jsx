// MediFlow / Admin / src / pages / ListService / ListService.jsx
import ListServicePage from "../../components/ListServicePage/ListServicePage";

const ListService = () => {
  return (
    <div>
      <ListServicePage />
    </div>
  );
};

export default ListService;
