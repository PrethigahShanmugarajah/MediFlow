// MediFlow / Admin / src / pages / Add / Add.jsx
import AddPage from "../../components/AddPage/AddPage";

const Add = () => {
  return (
    <div>
      <AddPage />
    </div>
  );
};

export default Add;
