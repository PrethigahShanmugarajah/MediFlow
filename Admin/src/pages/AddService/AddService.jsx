// MediFlow / Admin / src / pages / AddService / AddService.jsx
import Navbar from "../../components/Navbar/Navbar";
import AddServicePage from "../../components/AddServicePage/AddServicePage";

const AddService = () => {
  return (
    <div className="bg-linear-to-br from-indigo-50 via-indigo-100 to-fuchsia-50">
      <Navbar />
      <AddServicePage />
    </div>
  );
};

export default AddService;
